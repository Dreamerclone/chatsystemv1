<Window x:Class="ChatSystem.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:ChatSystem.ViewModels"
        xmlns:infra="clr-namespace:ChatSystem.Infrastructure"
        mc:Ignorable="d"
        Title="WPF Chat System" Height="500" Width="600" Background="#F0F2F5">

    <Window.Resources>
        <infra:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <infra:ConnectionStatusColorConverter x:Key="ConnectionStatusColorConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Connection Panel -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="User:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding Username}" Width="80" Margin="0,0,10,0" IsEnabled="{Binding IsConnected, Converter={StaticResource InverseBooleanConverter}}"/>

            <TextBlock Text="Host:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding ServerIp}" Width="120" Margin="0,0,10,0" IsEnabled="{Binding IsConnected, Converter={StaticResource InverseBooleanConverter}}"/>

            <TextBlock Text="Port:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding Port}" Width="50" Margin="0,0,10,0" IsEnabled="{Binding IsConnected, Converter={StaticResource InverseBooleanConverter}}"/>

            <Button Content="Connect" Command="{Binding ConnectCommand}" Padding="10,2"/>
        </StackPanel>

        <!-- Messages List -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" BorderBrush="#DDD" BorderThickness="1">
            <ItemsControl ItemsSource="{Binding Messages}">
                <ItemsControl.Template>
                    <ControlTemplate>
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsPresenter/>
                        </ScrollViewer>
                    </ControlTemplate>
                </ItemsControl.Template>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="5" Padding="10" CornerRadius="5" Background="#E7F3FF" HorizontalAlignment="Left" MaxWidth="400">
                            <StackPanel>
                                <TextBlock Text="{Binding Sender.Username}" FontWeight="Bold" Foreground="#1877F2" FontSize="11"/>
                                <TextBlock Text="{Binding Text}" TextWrapping="Wrap" Margin="0,2"/>
                                <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:HH:mm}'}" FontSize="9" Foreground="Gray" HorizontalAlignment="Right"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <!-- Input Panel -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged}" Grid.Column="0" Height="40" Padding="5" VerticalContentAlignment="Center" BorderThickness="1">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding SendCommand}"/>
                </TextBox.InputBindings>
            </TextBox>
            <Button Content="Send" Command="{Binding SendCommand}" Grid.Column="1" Margin="10,0,0,0" Padding="20,0" Background="#1877F2" Foreground="White" BorderThickness="0" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
    </Grid>
</Window>
